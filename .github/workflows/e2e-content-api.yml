name: E2E - Content API to Amplify

on:
  push:
    branches: [dev]
    paths: ['.github/run-e2e']

permissions:
  contents: write

jobs:
  e2e:
    runs-on: ubuntu-latest
    steps:
      - name: Ping with write permission
        run: echo "contents:write workflow started"
